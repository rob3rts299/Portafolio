let cadena = "cadena de prueba... prueba... prueba...";
let cadena2 = "prueba               " ;
let cadena3 = "abc"
let cadena4 = "hola, como estas? que tal tu tarde"
let cadena5 = "ABCEDSFOPE"
let cadena6 = 12


// resultado = cadena.concat("hola"); //añade otras cadenas
// resultado = cadena.startsWith(cadena2); //verifica si la cadena inicia con el comienzo o directamente con las otra cadena.
// resultado = cadena.endsWith(cadena2); //lo mismo que el ejemplo anterior pero al final
// resultado = cadena.includes(cadena2); //verifica si la cadena que se busca (cadena2) esta dentro de la cadena seleccionada (cadena)
// resultado = cadena.indexOf(cadena2);// nos muesta la posicion del inicio de la palabra que estamos buscando
// resultado = cadena.lastIndexOf(cadena2);//lo mismo que la anterior pero devuelve la ultima palabra de la que se busca
// resultado = cadena3.padStart(11,cadena2);//se marcan la cantidad de caracteres que quiere que se rellenen y luego se lo coloca con que se lo rellenera (cadena2)
// resultado = cadena3.padEnd(11,cadena2);//lo mismo que el anterior pero se pondra al final de la cadena
// resultado = cadena3.repeat(11);//repite la cadena las veces que se les indica
// resultado = cadena4.split("como");//divide la cadena en un array y le proporciona la posicion dependiendo del caracter divisior de dentro del parentesis
                // document.write(resultado[1])
// resultado = cadena5.substring(1,5);//genera un nuevo string desde la cadena colocando la posicion de inicio y final que le demos, muesta el caracter con el que inicia pero no el que finaliza
// resultado = cadena5.toLowerCase();//convierte toda la cadena a minuscula
// resultado = cadena4.toUpperCase();//convierte toda la cadena a mayuscula
// resultado = cadena6.toString();//convierte numeros en string, caso especial al sumar los concatena como un numero y string
// resultado = cadena2.trim();//elimina los espacios generados, para corroborar se usa el .length
// resultado = cadena2.trimEnd();//elimina los espacios del final de la cadena
// resultado = cadena2.trimStart();//elimina los espacios del inicio de la cadena





document.write(resultado.length)